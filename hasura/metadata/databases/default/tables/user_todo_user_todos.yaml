table:
  schema: user_todo
  name: user_todos
object_relationships:
  - name: user
    using:
      foreign_key_constraint_on: user_id
select_permissions:
  - role: guest
    permission:
      columns:
        - todo_id
        - user_id
        - description
        - title
        - created_at
        - finished_at
        - updated_at
      filter: {}
