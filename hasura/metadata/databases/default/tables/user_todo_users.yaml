table:
  name: users
  schema: user_todo
array_relationships:
- name: user_todos
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: user_todos
        schema: user_todo
