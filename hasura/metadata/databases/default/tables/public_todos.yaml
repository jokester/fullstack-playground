table:
  schema: public
  name: todos
select_permissions:
  - role: guest
    permission:
      columns:
        - todo_id
        - title
        - desc
        - finished_at
        - created_at
        - updated_at
      filter: {}
