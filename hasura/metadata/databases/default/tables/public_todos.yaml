table:
  name: todos
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - todo_id
        - title
        - desc
        - finished_at
        - created_at
        - updated_at
      filter: {}
