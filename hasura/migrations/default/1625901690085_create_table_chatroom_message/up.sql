CREATE TABLE "chatroom"."message" ("message_id" bigserial NOT NULL, "user_id" Integer NOT NULL, "room_id" integer NOT NULL, "content" text NOT NULL, PRIMARY KEY ("message_id") , FOREIGN KEY ("room_id") REFERENCES "chatroom"."room"("room_id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("user_id") REFERENCES "chatroom"."user"("user_id") ON UPDATE restrict ON DELETE restrict);
